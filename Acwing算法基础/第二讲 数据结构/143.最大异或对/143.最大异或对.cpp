#include<iostream>
#include<bitset>
#include<math.h>
using namespace std;
const int N=3100010;

long long int son[N][2],cnt[N],idx;

void insert(int x){
	string s=bitset<10>(x).to_string();
}

int main(){
	//<bitset<10>(pow(2,31))
	long long int i=pow(2,31);
	cout<<i;
	return 0; 
}
