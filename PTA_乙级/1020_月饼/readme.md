1020.月饼
==
https://pintia.cn/problem-sets/994805260223102976/exam/problems/type/7?problemSetProblemId=994805301562163200&page=0

考查多重背包问题。但是这里似乎简单的贪心策略也是奏效的。

对于new声明的结构体数组，取某个元素的对象还是应该使用.而不是->

对于struct使用sort时，给出需要排序的地址范围，比如sort(str,str+l,cmp)。